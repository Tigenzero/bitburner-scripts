servers = ['sigma-cosmetics', 'joesguns', 'harakiri-sushi', 'max-hardware', 'hong-fang-tea']

for (i in servers){
	server = servers[i]
	if( serverExists(server)){
		run('weaken.script',1, server)
		run('grow.script',1, server)
		run('hack.script',1, server)
	}
}